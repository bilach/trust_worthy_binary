name: Build and Release Hello World

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build Hello World executable
        shell: cmd
        run: cl.exe /nologo /O2 /Fe:npp.8.9.3.Installer.x64.exe hello.c

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: hello-world-windows
          path: npp.8.9.3.Installer.x64.exe

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          files: npp.8.9.3.Installer.x64.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
